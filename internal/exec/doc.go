// Package exec implements Layer 7 — the execution engine: tool registry,
// dispatcher, sandbox, HITL approval, and observation normalization
// (File 08).
//
// Allowed imports: event, patch, infra.
package exec
