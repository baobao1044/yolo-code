// Package session implements Layer 1 — Session/Task lifecycle, checkpoints,
// undo stack, resume, and cancel-vs-pause semantics (File 03).
//
// Allowed imports: event, infra (Cost only via interface).
package session
