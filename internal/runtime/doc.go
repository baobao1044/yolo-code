// Package runtime implements Layer 2 — the runtime finite-state machine that
// drives a single turn on one goroutine (File 04).
//
// Allowed imports: session, event, infra, exec, verify, patch (interfaces).
package runtime
